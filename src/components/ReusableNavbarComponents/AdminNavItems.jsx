export default function AdminNavItems({ navigate }) {
    return (
        <>
        {/* <button onClick={() => navigate('/employee-tasks')} className="text-gray-300 hover:text-white">Dashboard</button> */}
        {/* <button onClick={() => navigate('/employee-reports')} className="text-gray-300 hover:text-white">Employee Details</button> */}
        </>
    );
}